def lazy(search):
    lazy_hash = ord(search[0]) * 900 + ord(search[1]) * 30 + ord(search[2])
    print(lazy_hash)

    # Hasha alla ord.
    # skriv alla ord i fil. (en gång per ord) och sedan alla index de förekommer på.
    # Korpus har väl alla?
    return


def main():
    print("hello")
    lazy("ööö")


main()
